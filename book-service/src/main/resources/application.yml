server:
  port: ${SERVER_PORT:8082}

spring:
  application:
    name: book-service
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/bookstore}

jwt:
  secret: ${JWT_SECRET:bookstore-devops-jwt-secret-key-2026-min-32-chars}
